# Production must be selected explicitly with SPRING_PROFILES_ACTIVE=prod.
spring.datasource.url=${SUPABASE_PROD_DB_URL:\
  jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${SUPABASE_PROD_DB_USERNAME:postgres.iozaniwbftbrslqzqfxk}
spring.datasource.password=${SUPABASE_PROD_DB_PASSWORD}

# Never change production tables implicitly. Apply schema changes deliberately first.
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:validate}
spring.jpa.show-sql=false
