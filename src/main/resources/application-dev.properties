# Development is the safe default profile and targets the dedicated DEV project.
spring.datasource.url=${SUPABASE_DEV_DB_URL:${SUPABASE_DB_URL:\
  jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:5432/postgres?sslmode=require}}
spring.datasource.username=${SUPABASE_DEV_DB_USERNAME:${DB_USERNAME:postgres.rrxvxcufuwmjbqiwhgmt}}
spring.datasource.password=${SUPABASE_DEV_DB_PASSWORD:${DB_PASSWORD:}}

# Schema updates are acceptable for the JPA laboratory in the isolated DEV database.
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
